{
  "name": "studus-automatizado-monorepo",
  "version": "1.0.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "private": true,
  "scripts": {
    "dev": "bun run --filter '*' dev",
    "build": "bun run --filter '*' build",
    "test": "bun test",
    "clean": "rm -rf node_modules apps/*/node_modules packages/*/node_modules"
  },
  "devDependencies": {
    "bun-types": "latest",
    "playwright": "^1.57.0"
  },
  "dependencies": {
    "@types/bcrypt": "^6.0.0",
    "@types/jsonwebtoken": "^9.0.10",
    "@types/uuid": "^11.0.0",
    "bcrypt": "^6.0.0",
    "clsx": "^2.1.1",
    "jsonwebtoken": "^9.0.3",
    "tailwind-merge": "^3.4.0",
    "uuid": "^13.0.0"
  }
}
